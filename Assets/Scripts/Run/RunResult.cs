using UnityEngine;

// Carries the finished run across the scene transition:
// GameScene writes the results, the GameOver scene reads and displays them
public static class RunResult
{
    public static float Score;
}
